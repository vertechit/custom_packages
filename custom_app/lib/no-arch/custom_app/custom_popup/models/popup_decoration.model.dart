import 'package:flutter/material.dart';

class PopupDecoration {
  Color backgroundColor;
  Color titleColor;
  Color textColor;
  PopupDecoration({
    this.backgroundColor,
    this.titleColor,
    this.textColor,
  });
}
